// Módulo mode — los componentes de selección de modo se implementan
// directamente en las páginas (src/app/(protected)/mode-select/page.tsx,
// preferences/page.tsx, describe/page.tsx) para evitar over-engineering.
// Este archivo reserva el directorio para futuras extracciones si se necesitan.
